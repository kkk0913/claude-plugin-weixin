{
  "mcpServers": {
    "weixin": {
      "command": "bun",
      "args": ["run", "--cwd", "${CLAUDE_PLUGIN_ROOT}", "--silent", "server.ts"],
      "env": {
        "WEIXIN_SERVER_ROLE": "proxy"
      }
    }
  }
}
